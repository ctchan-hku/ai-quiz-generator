---
phase: 06-few-shot-math-model
plan: 01
title: "Backend — few_shot_examples + gpt-4.1 catalog"
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/routers/generate.py
  - backend/app/services/llm.py
  - backend/.env.example
autonomous: true
requirements:
  - MOD-01
  - MOD-02
  - AI-FS-01
  - AI-FS-02
  - AI-FS-03
  - DEPLOY-04
---

<objective>
Extend the generate API and LLM message builder for optional `few_shot_examples`, and add `gpt-4.1` to the default model list and env example. Downstream frontend plans consume the same request shape.
</objective>

<tasks>

<task id="1">
<type>execute</type>
<title>Config — append gpt-4.1 to fallback models</title>
<read_first>
- backend/app/config.py (`_FALLBACK_MODELS`)
- .planning/phases/06-few-shot-math-model/06-CONTEXT.md (D-6-01, D-6-02)
</read_first>
<action>
Append `{"id": "gpt-4.1", "label": "GPT-4.1 (math & reasoning)"}` to `_FALLBACK_MODELS` after `gpt-4o`, preserving existing entries.
</action>
<acceptance_criteria>
- `_FALLBACK_MODELS` includes an entry with `id` exactly `gpt-4.1` and the label from CONTEXT.
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Env example + README — AVAILABLE_MODELS documents gpt-4.1</title>
<read_first>
- backend/.env.example
- README.md (deployment / env section if present)
- .planning/phases/06-few-shot-math-model/06-CONTEXT.md (D-6-02, DEPLOY-04)
</read_first>
<action>
Update `backend/.env.example` `AVAILABLE_MODELS` JSON to include the three models (mini, 4o, 4.1) with labels matching `_FALLBACK_MODELS`. Add a short comment that operators must confirm the model id with their OpenAI-compatible provider.

Update project `README.md` in the same spirit if it documents `AVAILABLE_MODELS` (do not invent sections — only extend existing env/deploy documentation).
</action>
<acceptance_criteria>
- `backend/.env.example` sample JSON includes `gpt-4.1` with the math/reasoning label.
- Provider-id caveat appears in `.env.example` comment and README if README already covers env vars.
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Normalize few_shot_examples helper + GenerateTextRequest</title>
<read_first>
- backend/app/routers/generate.py
- .planning/phases/06-few-shot-math-model/06-CONTEXT.md (D-6-03 … D-6-05)
</read_first>
<action>
Add optional `few_shot_examples: list[str] | None = None` to `GenerateTextRequest` (or `Field(default=None)`).

Implement `normalize_few_shot_examples(raw: list[str] | None) -> list[str]` in a sensible module (`generate.py` or a small `app/services/few_shot.py` — prefer colocate with generate if single consumer):

- If `raw` is `None`, return `[]`.
- Trim each string; drop empty after trim.
- If more than 3 non-empty remain, raise `HTTPException(422, detail=...)`.
- If any non-empty string length > 2000, raise `HTTPException(422, detail=...)`.

In `generate_text`, compute `examples = normalize_few_shot_examples(body.few_shot_examples)` and pass to `generate_quiz` (next task). Do not require the client to omit the field — `[]` after normalize means no few-shot block.
</action>
<acceptance_criteria>
- Request accepts omitted `few_shot_examples`, `null`, `[]`, and valid lists; invalid counts/lengths return 422.
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>
<title>llm.build_messages + generate_quiz — inject few-shot system section</title>
<read_first>
- backend/app/services/llm.py
- .planning/phases/06-few-shot-math-model/06-CONTEXT.md (D-6-06 … D-6-08)
</read_first>
<action>
Extend `build_messages` with `few_shot_examples: list[str] | None = None` (caller passes **already normalized** list from router; use empty list as “no block”).

When `few_shot_examples` is non-empty, append to the system content a dedicated section per **D-6-07** (user-authored style examples; match tone/difficulty/format; do not copy verbatim; numbered examples with verbatim user text).

Extend `generate_quiz` to accept the same parameter and forward to `build_messages`.

Wire `generate_text` to pass `examples` from the normalizer into `generate_quiz`.
</action>
<acceptance_criteria>
- Initial completion and `parse_with_retry` share identical `messages` including few-shot content when examples are provided.
- When `examples` is empty, system prompt matches pre-phase-6 behavior aside from unrelated edits.
</acceptance_criteria>
</task>

<task id="5">
<type>execute</type>
<title>Backend verification</title>
<read_first>
- backend/tests/ if any cover generate
</read_first>
<action>
Run backend tests (`pytest` from `backend/`). If no tests exist for generate, add a minimal unit test for `normalize_few_shot_examples` (or the validation path) covering: empty, trim/drop, max 3, max length 2000.

Manual smoke: `POST /api/generate/text` with valid payload still returns 200; with four non-empty examples returns 422.
</action>
<acceptance_criteria>
- `pytest` passes.
</acceptance_criteria>
</task>

</tasks>

<verification>
- `curl` or httpx local smoke for generate with and without `few_shot_examples`.
</verification>

<success_criteria>
MOD-01, MOD-02, AI-FS-01…03, DEPLOY-04 satisfied on the backend; README/.env.example aligned with catalog.
</success_criteria>
