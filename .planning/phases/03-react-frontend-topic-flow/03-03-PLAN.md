---
phase: 03-react-frontend-topic-flow
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/main.tsx
  - src/App.tsx
  - vercel.json
autonomous: false
requirements: [FE-07, DEPLOY-02]
user_setup:
  - service: Vercel
    why: "Frontend Hosting"
    env_vars:
      - name: VITE_API_BASE_URL
        source: "Set to Railway backend URL"

must_haves:
  truths:
    - "Application integrates Form, Display, and State Machine into one seamless view"
    - "The frontend is deployed to Vercel and successfully communicates with the Railway backend"
  artifacts:
    - path: "src/App.tsx"
      provides: "Main integration container"
    - path: "vercel.json"
      provides: "Vercel SPA rewrite configuration"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useQuizMachine.ts"
      via: "Hooks consumption and component mounting"
      pattern: "useQuizMachine\\("
---

<objective>
Wire the Form, Display, and State components together into the main App layout, and deploy the finished React application to Vercel.

Purpose: Delivers the fully functional end-to-end Topic Flow phase by bringing all pieces together and making it publicly accessible.
Output: Integrated `App.tsx` and a Vercel-hosted URL.
</objective>

<execution_context>
@C:/Users/noahc/Documents/projects/talic/mastery-exec/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/noahc/Documents/projects/talic/mastery-exec/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@design-system/ai-quiz-generator/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App Integration and Provider Setup</name>
  <files>src/main.tsx, src/App.tsx, vercel.json</files>
  <action>
    In `main.tsx`: Wrap the `<App />` in a TanStack Query `QueryClientProvider` and React StrictMode.
    In `App.tsx`:
    - Layout: A central column container with standard `lg` padding. A top header/logo area.
    - Render `QuizForm` persistently at the top.
    - Consume `useQuizMachine` state:
      - If `status === 'idle'`: Render `EmptyState`.
      - If `status === 'generating'`: Render `LoadingState`.
      - If `status === 'reviewing'`: Render `QuizDisplay`. Explicitly specify passing the quiz data to QuizDisplay as props in the render conditions.
      - If `status === 'error'`: Render `ErrorState`. Explicitly specify passing the error state string to ErrorState as a prop in the render conditions.
    Create `vercel.json` with a standard rewrite array for SPAs (`source: "/(.*)", destination: "/index.html"`).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>App renders the full flow locally based on the state machine.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Vercel Deployment</name>
  <what-built>The Vercel deployment configuration is set up and the codebase is ready.</what-built>
  <how-to-verify>
    1. Deploy this frontend branch/project to Vercel via Vercel CLI or Dashboard.
    2. Set `VITE_API_BASE_URL` to your production Railway backend URL.
    3. Visit the Vercel URL, type a topic (e.g., "Photosynthesis"), and click "Generate Quiz".
    4. Ensure the generating animation appears and the quiz displays successfully.
  </how-to-verify>
  <resume-signal>Type "approved" once the Vercel URL works end-to-end, or describe any CORS/deployment issues to fix.</resume-signal>
  <done>Frontend deployed and verified against the live backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CORS | Backend API accepting requests from the Vercel Frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing | Deployment | mitigate | Ensure Vercel domain is added to `ALLOWED_ORIGINS` in Railway environment to enforce CORS |
</threat_model>

<verification>
Check that the `vercel.json` file is correctly structured to serve the `index.html` file on all paths.
</verification>

<success_criteria>
The Topic Flow is fully functional, integrated, and deployed to a public URL.
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-frontend-topic-flow/03-03-SUMMARY.md`
</output>