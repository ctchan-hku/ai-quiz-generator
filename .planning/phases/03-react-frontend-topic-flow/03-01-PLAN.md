---
phase: 03-react-frontend-topic-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - index.html
  - src/index.css
  - src/types/quiz.ts
  - src/lib/api.ts
  - src/hooks/useQuizMachine.ts
autonomous: true
requirements: [FE-03]
user_setup: []

must_haves:
  truths:
    - "Application visually applies the Claymorphism design system fonts and colors"
    - "State machine can transition between idle, generating, reviewing, and exporting"
  artifacts:
    - path: "src/index.css"
      provides: "Claymorphism design system variables"
    - path: "src/types/quiz.ts"
      provides: "TypeScript types matching backend discriminated unions"
    - path: "src/hooks/useQuizMachine.ts"
      provides: "Quiz state management via useReducer and TanStack Query"
  key_links:
    - from: "src/hooks/useQuizMachine.ts"
      to: "src/lib/api.ts"
      via: "TanStack Query useMutation for API calls"
      pattern: "useMutation\\(.*generateQuiz"
---

<objective>
Scaffold the React application, establish the Claymorphism design system, and implement the core state machine for the quiz generation flow.

Purpose: Provides the foundation (types, API client, state machine, and CSS) that all UI components will build upon.
Output: Vite+React boilerplate, design tokens in CSS, API client, and `useQuizMachine` hook.
</objective>

<execution_context>
@C:/Users/noahc/Documents/projects/talic/mastery-exec/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/noahc/Documents/projects/talic/mastery-exec/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/ai-quiz-generator/MASTER.md
@.planning/phases/03-react-frontend-topic-flow/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite App and Design System</name>
  <files>package.json, vite.config.ts, index.html, src/index.css</files>
  <action>
    Scaffold Vite + React 19 + TypeScript. Install Tailwind v4, lucide-react, axios, and @tanstack/react-query.
    In `src/index.css`, implement the Claymorphism design system from the UI Spec:
    - Fonts: Import Baloo 2 (Headings) and Comic Neue (Body) from Google Fonts.
    - Colors: Primary #4F46E5, Secondary #818CF8, Accent #F97316, Background #EEF2FF, Text #1E1B4B.
    - Spacing & Shadows: Add CSS variables for spacing (xs to 3xl) and shadows (sm to xl) as specified in MASTER.md.
    - Base Component Classes: Add `.btn-primary`, `.btn-secondary`, `.card`, and `.input` utility classes matching the soft 3D/bubbly claymorphism rules.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Vite builds successfully; CSS contains all design tokens and font imports.</done>
</task>

<task type="auto">
  <name>Task 2: Define Types and API Client</name>
  <files>src/types/quiz.ts, src/lib/api.ts</files>
  <action>
    Create `src/types/quiz.ts` mirroring the backend schema:
    - `QuestionBase` with `question` and `explanation`.
    - `MultipleChoiceQuestion` (extends base) with `question_type: "multiple_choice"`, `options`, `correct_indices`.
    - `QuizQuestion` discriminated union (only `multiple_choice` for now).
    - `QuizResponse` containing `questions: QuizQuestion[]`.
    
    Create `src/lib/api.ts`:
    - Setup Axios client using `import.meta.env.VITE_API_BASE_URL` (fallback to `/api`).
    - Create `generateQuiz(topic: string, numQuestions: number, model: string)` function calling `POST /generate/text`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript compiles without errors; API client exports typed functions.</done>
</task>

<task type="auto">
  <name>Task 3: Build State Machine Hook</name>
  <files>src/hooks/useQuizMachine.ts</files>
  <action>
    Implement `useQuizMachine` using `useReducer` and TanStack Query's `useMutation`.
    - State interface: `{ status: 'idle' | 'generating' | 'reviewing' | 'exporting' | 'error', formConfig: { topic, numQuestions, model }, quiz: QuizResponse | null, error: string | null }`.
    - Actions: `START_GENERATE` (sets status to generating and updates formConfig), `GENERATE_SUCCESS` (sets quiz and status to reviewing), `GENERATE_ERROR` (sets error).
    - Expose the state and the `useMutation` triggers so UI components can dispatch actions and react to state changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Hook is fully typed and exports state and mutation handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User inputs topic string that is sent to the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Input/Injection | API Client | mitigate | Rely on backend Pydantic validation; do not blindly render HTML from API (use React safe text rendering) |
</threat_model>

<verification>
Check that `npm run build` succeeds and `npm run dev` starts successfully.
</verification>

<success_criteria>
App scaffolds, compiles, and provides types and state management ready for UI integration.
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-frontend-topic-flow/03-01-SUMMARY.md`
</output>