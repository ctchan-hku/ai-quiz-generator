---
phase: 03-react-frontend-topic-flow
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/QuizForm.tsx
  - src/components/QuizDisplay.tsx
  - src/components/LoadingState.tsx
  - src/components/ErrorState.tsx
  - src/components/EmptyState.tsx
autonomous: true
requirements: [FE-04, FE-05, FE-06]
user_setup: []

must_haves:
  truths:
    - "User types a topic, clicks Generate, and the button disables"
    - "Stepped loading states (Generating questions...) appear immediately on submit"
    - "Quiz review shows numbered questions with A/B/C/D labeled options and explanations"
    - "Empty state shows 3 example prompts, and errors show readable messages"
  artifacts:
    - path: "src/components/QuizForm.tsx"
      provides: "Topic textarea and question count select"
    - path: "src/components/QuizDisplay.tsx"
      provides: "Card-based Bento grid for rendering the quiz response"
  key_links:
    - from: "src/components/QuizDisplay.tsx"
      to: "QuizResponse"
      via: "Maps over questions to render cards"
      pattern: "questions\\.map"
---

<objective>
Implement the UI components for the Quiz Form, the Quiz Display, and the various application states (Loading, Empty, Error) adhering strictly to the Claymorphism design system.

Purpose: Creates the user interface elements necessary for topic input and quiz review.
Output: Core React components ready for integration into the main App layout.
</objective>

<execution_context>
@C:/Users/noahc/Documents/projects/talic/mastery-exec/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/noahc/Documents/projects/talic/mastery-exec/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@design-system/ai-quiz-generator/MASTER.md
@.planning/phases/03-react-frontend-topic-flow/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build QuizForm and State Components</name>
  <files>src/components/QuizForm.tsx, src/components/LoadingState.tsx, src/components/EmptyState.tsx, src/components/ErrorState.tsx</files>
  <action>
    Build `QuizForm.tsx`: A form containing a textarea for the topic, a selector for question count (5/10/15/20), and a primary "Generate Quiz" button. The button must disable when `isLoading` is true.
    Build `LoadingState.tsx`: Playful skeleton loaders (`animate-pulse` with rounded bubbly shapes). Add stepped text "Generating questions...".
    Build `EmptyState.tsx`: Shows heading "What do you want to learn about?" and 3 example prompts the user can click to auto-fill the form.
    Build `ErrorState.tsx`: Displays a Destructive color alert ("Oops! Something went wrong...").
    Ensure all components use the `.card`, `.btn-primary`, and `.input` CSS classes created in Plan 01.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All state and form components compile and apply the correct design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Build QuizDisplay Component</name>
  <files>src/components/QuizDisplay.tsx</files>
  <action>
    Build `QuizDisplay.tsx` to render the `QuizResponse`.
    - Map over `questions` (assume all are `MultipleChoiceQuestion` for now).
    - Use a Card-based Bento grid layout (1 question per distinct block/card with depth).
    - Each card displays the numbered question and four A/B/C/D labeled options.
    - Implement answer reveal behavior: when an option is clicked, immediately reveal the correct answer with a playful micro-interaction (e.g., green flash for correct, red for incorrect).
    - Display the `explanation` text beneath the options after an answer is revealed.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>QuizDisplay maps over questions and handles local reveal state correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component | User views API response data in QuizDisplay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | XSS | QuizDisplay | mitigate | React automatically escapes string variables; do not use `dangerouslySetInnerHTML` for question or explanation strings |
</threat_model>

<verification>
Check that all components are fully typed and use the provided CSS classes without introducing horizontal scroll or layout shift.
</verification>

<success_criteria>
Quiz form and display components exist and accurately reflect the Claymorphism UI specs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-frontend-topic-flow/03-02-SUMMARY.md`
</output>