---
phase: 05-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/exportJournal.ts
  - frontend/src/lib/formatQuizPlainText.ts
autonomous: true
requirements: [EXP-02, EXP-03]
user_setup: []

must_haves:
  truths:
    - "Journal JSON matches schema in 05-CONTEXT.md (schema_version, updated_at, quizzes[] with per-question comment)"
    - "Persistence uses a single localStorage key; clearJournal removes it; load handles corrupt JSON safely"
  artifacts:
    - path: "frontend/src/lib/exportJournal.ts"
      provides: "Types, load/save/append/clear, buildQuizExportRecord, trigger download"
    - path: "frontend/src/lib/formatQuizPlainText.ts"
      provides: "Plain-text quiz string for clipboard (EXP-01)"
  key_links:
    - from: "frontend/src/lib/exportJournal.ts"
      to: "frontend/src/types/quiz.ts"
      via: "QuizResponse / QuizQuestion"
      pattern: "import type"
---

<objective>
Implement **pure TypeScript** helpers for the export journal (localStorage), building **`QuizExportRecord`** from `QuizResponse` + per-question comments + topic, and **plain-text formatting** for clipboard. No React in this wave.

Purpose: Keeps persistence and formatting **testable and small** (Karpathy: minimum code; clean-code: one level of abstraction per module).
Output: `exportJournal.ts`, `formatQuizPlainText.ts`; `npm run build` passes.
</objective>

<context>
@.planning/phases/05-export/05-CONTEXT.md
@.planning/REQUIREMENTS.md
@frontend/src/types/quiz.ts
@frontend/src/components/QuizDisplay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export journal module</name>
  <files>frontend/src/lib/exportJournal.ts</files>
  <action>
    - Define types aligned with **D-20 / D-21** in `05-CONTEXT.md`: `ExportJournal`, `QuizExportRecord`, `QuizExportQuestion` (include `index`, `question_type`, `question`, `options?`, `correct_indices?`, `explanation`, `comment`).
    - **Constant** `EXPORT_JOURNAL_STORAGE_KEY = 'mastery-exec-quiz-export-journal'` (single source; no duplicate strings elsewhere).
    - `loadJournal(): ExportJournal` — if missing or invalid JSON, return `{ schema_version: 1, updated_at: new Date().toISOString(), quizzes: [] }` (do not throw on corrupt data).
    - `saveJournal(j: ExportJournal): void` — set `updated_at` to now before `localStorage.setItem`.
    - `appendQuizRecord(record: QuizExportRecord): ExportJournal` — load, push to `quizzes`, save, return updated journal.
    - `clearJournal(): void` — `removeItem` key.
    - `buildQuizExportRecord(params: { quiz: QuizResponse; topic: string; commentsByIndex: string[] }): QuizExportRecord` — for each question, attach trimmed `comment` (default `''`); set `exported_at` ISO; copy `model_used`, `source`, `truncated` from quiz; **index** = array position.
    - `downloadJournalFile(journal: ExportJournal, filename?: string)` — default filename **`quiz-export-journal.json`**; `Blob` + object URL + programmatic anchor with `download` attribute, then `revokeObjectURL`.
    - Keep functions **short**; no speculative features (no IndexedDB unless you hit quota — then catch `QuotaExceededError` and rethrow a clear message for UI layer).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run build</automated>
  </verify>
  <done>Build passes; module exports all functions and types needed by `ExportPanel`.</done>
</task>

<task type="auto">
  <name>Task 2: Plain text formatter</name>
  <files>frontend/src/lib/formatQuizPlainText.ts</files>
  <action>
    - Export `formatQuizPlainText(quiz: QuizResponse, topic?: string, commentsByIndex?: string[]): string`.
    - **MCQ** (`question_type === 'multiple_choice'`): match **D-04** — numbering, options labeled **A–D** same order as `QuizDisplay` (`LABELS` array), **Answer:** correct letter(s), **Explanation:** if non-empty.
    - **Non-MC**: **D-05** — one readable stub line with type + question text.
    - Optional header **D-06**: topic (if provided), `model_used`, `source`.
    - If `commentsByIndex[i]` non-empty after trim, append **Notes:** line for that question (implements **D-19** choice to include notes in clipboard).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run build</automated>
  </verify>
  <done>Build passes; formatter is deterministic and uses only `QuizResponse` types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser storage | Journal is client-only; no secrets |

## STRIDE (lightweight)

| Threat | Mitigation |
|--------|------------|
| Corrupt localStorage JSON | `loadJournal` returns empty journal — **D-14** |
| Huge paste / storage quota | Trim comments; catch quota errors at UI if needed in wave 2 |
</threat_model>

<verification>
`cd frontend && npm run build` succeeds.
</verification>

<success_criteria>
Journal helpers and plain-text export compile and match `05-CONTEXT.md` schema and labeling rules.
</success_criteria>

<output>
After completion, create `.planning/phases/05-export/05-01-SUMMARY.md`
</output>
