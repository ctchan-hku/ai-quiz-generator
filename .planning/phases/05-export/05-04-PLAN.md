---
phase: 05-export
plan: 04
type: execute
wave: 4
depends_on: [05-03-PLAN.md]
files_modified:
  - frontend/src/components/ExportPanel.tsx
  - frontend/src/lib/quiz-export/journal.ts
autonomous: true
requirements: [FE-08, EXP-02, EXP-03]
user_setup: []

must_haves:
  truths:
    - "Instruction text only specifies what the notes are used for."
    - "'Plain text' terminology is replaced with 'clipboard format' (or similar) in UI and code."
    - "A dedicated journal section exists with Record, Clear, and Export buttons."
    - "The journal section displays a list of recorded quizzes, each with a remove button."
  artifacts:
    - path: "frontend/src/components/ExportPanel.tsx"
      provides: "Updated text, renamed preview variables, and a new journal management section."
    - path: "frontend/src/lib/quiz-export/journal.ts"
      provides: "removeQuizRecord function to support deleting individual items."
---

<objective>
Refine the `ExportPanel` UI by clarifying instruction text, removing vague "plain text" terminology, and introducing a dedicated journal management section with a list of recorded quizzes and individual remove actions.
</objective>

<context>
@frontend/src/components/ExportPanel.tsx
@frontend/src/lib/quiz-export/journal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update journal.ts</name>
  <files>frontend/src/lib/quiz-export/journal.ts</files>
  <action>
    - Export a new function `removeQuizRecord(index: number): ExportJournal`.
    - Inside `removeQuizRecord`, load the journal, remove the item at `index` from the `quizzes` array, save the journal, and return the updated journal.
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <manual>Function is exported and correctly modifies the array.</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Refactor instruction text and preview terminology</name>
  <files>frontend/src/components/ExportPanel.tsx</files>
  <action>
    - Update the top `<p>` instruction text to only specify what the notes are used for (e.g., "Add optional notes per question to jot down your review, what's good or bad, or how it satisfies your needs."). Remove references to viewing plain text, appending, or downloading from this paragraph.
    - Rename state `isPlainTextPreviewOpen` to `isQuizSummaryPreviewOpen`.
    - Rename `plainTextPreview` to `quizSummaryPreviewText`.
    - Rename handlers `handleTogglePlainTextPreview` to `handleToggleQuizSummaryPreview`.
    - Update the button text from "View plain text" / "Hide plain text" to "Preview quiz summary" / "Hide quiz summary".
    - Update the hint text below the button to: "Preview shows the quiz summary text that will be copied to your clipboard (including your notes)."
    - Update the preview panel header from "Plain text preview" to "Quiz summary preview".
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <manual>UI text is updated and "plain text" is no longer used.</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Create Journal Management Section</name>
  <files>frontend/src/components/ExportPanel.tsx</files>
  <action>
    - Add a new state `journal` to hold the full journal object: `const [journal, setJournal] = useState(() => loadJournal());`.
    - Update `refreshJournalCount` to `refreshJournal` which sets the full journal state.
    - Create a new `<section>` or `<div>` at the bottom of `ExportPanel` for "Export Journal".
    - Move the "Clear journal" button into this section.
    - Change the "Append & download JSON" button behavior: split it into two buttons.
      1. "Record to journal" (primary button in the main area or top of journal section) — calls `appendQuizRecord`, then `refreshJournal()`, but does NOT download.
      2. "Export journal" (button in the journal section) — calls `downloadJournalFile(journal)`.
    - Render a list of `journal.quizzes` in the new section. For each quiz, display its `topic` and the number of questions, along with a "Remove" button.
    - The "Remove" button calls `removeQuizRecord(index)` and then `refreshJournal()`.
    - Ensure the layout is clean and follows existing Tailwind/shadcn patterns (e.g., flex rows, borders, text-sm).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <manual>Record adds to the list without downloading. Export downloads the file. Remove deletes an item. Clear empties the list.</manual>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Storage | Journal data is stored locally; user controls their own data. |

## STRIDE (lightweight)

| Threat | Mitigation |
|--------|------------|
| Data loss | Clear journal still requires a confirmation prompt. |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint`
- Manual: Verify text changes. Record a quiz, see it appear in the list. Remove it. Record again, export, verify JSON. Clear journal.
</verification>

<success_criteria>
Instruction text is focused. "Plain text" is removed. Journal has a dedicated section with Record, Export, Clear, and individual Remove actions.
</success_criteria>
