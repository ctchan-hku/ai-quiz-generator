---
phase: 05-export
plan: 02
type: execute
wave: 2
depends_on: [05-01-PLAN.md]
files_modified:
  - frontend/src/components/ExportPanel.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03]
user_setup: []

must_haves:
  truths:
    - "Review screen shows ExportPanel with per-question comment fields, Copy plain text, append+download JSON, Clear journal (confirm)"
    - "Handlers use useCallback per frontend guidelines; no unnecessary refactors outside these files"
  artifacts:
    - path: "frontend/src/components/ExportPanel.tsx"
      provides: "Export UI + clipboard + journal actions"
    - path: "frontend/src/App.tsx"
      provides: "Passes topic + quiz into ExportPanel when reviewing"
  key_links:
    - from: "frontend/src/components/ExportPanel.tsx"
      to: "frontend/src/lib/exportJournal.ts"
      via: "appendQuizRecord, downloadJournalFile, clearJournal, buildQuizExportRecord"
      pattern: "named imports"
---

<objective>
Add **`ExportPanel`** on the review path and wire **`App.tsx`** with `topic` + `quiz`. Deliver **EXP-01** (clipboard), **EXP-02/03** (journal download with comments). Surgical edits only (Karpathy: do not refactor unrelated components).

Purpose: User-visible export flow matching `05-CONTEXT.md`.
Output: Working UI; `npm run build` passes.
</objective>

<context>
@.planning/phases/05-export/05-CONTEXT.md
@frontend/src/lib/exportJournal.ts
@frontend/src/lib/formatQuizPlainText.ts
@frontend/src/hooks/useQuizMachine.ts
@.cursor/rules/frontend/guidelines.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExportPanel component</name>
  <files>frontend/src/components/ExportPanel.tsx</files>
  <action>
    - Props: `quiz: QuizResponse`, `topic: string`.
    - Local state: `comments: string[]` initialized to `quiz.questions.map(() =&gt; '')`; **reset when `quiz` reference changes** (useEffect with `[quiz]` dependency) so a new generation clears comments.
    - UI (**D-01, D-02, D-17**): collapsible or always-visible panel in a `card`; heading e.g. **Export / notes**; for each question, a labeled `textarea` (or `input`) bound to `comments[i]`; use `id`/`htmlFor` for accessibility.
    - **Copy** button: `formatQuizPlainText(quiz, topic, comments)` → `navigator.clipboard.writeText`; **D-11** on failure show `role="alert"` inline; on success **D-03** short inline “Copied!” ~2s.
    - **Download JSON** button: `buildQuizExportRecord({ quiz, topic, commentsByIndex: comments })` → `appendQuizRecord` logic: call `appendQuizRecord` from journal module (or load-append-save as implemented) → `downloadJournalFile` with full journal.
    - **Clear journal** button: `window.confirm` per **D-15** explaining data is local to browser → `clearJournal()`.
    - Optional: show `quizzes.length` from `loadJournal()` as hint (“Journal contains N quiz(es)”).
    - **GUIDE-01**: extract click handlers with `useCallback`; **GUIDE-02** hook order.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run build</automated>
  </verify>
  <done>Component compiles; all actions wired.</done>
</task>

<task type="auto">
  <name>Task 2: App integration</name>
  <files>frontend/src/App.tsx</files>
  <action>
    - When `state.status === 'reviewing' && state.quiz`, render **both** `QuizDisplay` and **`ExportPanel`** with `quiz={state.quiz}` and `topic={topic}` (**D-12**).
    - Do **not** change `QuizDisplay.tsx` unless required; prefer sibling `ExportPanel` below or above grid per layout taste (minimal diff).
    - Optional **D-10**: dispatch `ENTER_EXPORTING` / `EXIT_EXPORTING` around async clipboard only if you need global disable — default skip if local button `disabled` is enough.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run build</automated>
    <manual>Generate a quiz; add comments; download JSON twice; confirm second file contains two records; Clear journal resets; Copy includes notes when filled.</manual>
  </verify>
  <done>Build passes; manual smoke matches acceptance above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clipboard | Requires secure context / user gesture — surface errors |

## STRIDE (lightweight)

| Threat | Mitigation |
|--------|------------|
| XSS from quiz text | React escapes text nodes; do not use `dangerouslySetInnerHTML` |
</threat_model>

<verification>
- `cd frontend && npm run build`
- Manual: export flow and journal append (**05-CONTEXT** acceptance)
</verification>

<success_criteria>
EXP-01 / EXP-02 / EXP-03 behaviors visible on review screen; journal persists across exports in same browser.
</success_criteria>

<output>
After completion, create `.planning/phases/05-export/05-02-SUMMARY.md`
</output>
