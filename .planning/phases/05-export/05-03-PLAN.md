---
phase: 05-export
plan: 03
type: execute
wave: 3
depends_on: [05-02-PLAN.md]
files_modified:
  - frontend/src/components/ExportPanel.tsx
autonomous: true
requirements: [FE-08, EXP-01]
user_setup: []

must_haves:
  truths:
    - "User opens plain text via an explicit View control; preview shows output of buildQuizClipboardText(quiz, topic, comments)"
    - "Copy to clipboard control appears inside the preview panel (not only as a top-level action beside JSON)"
    - "JSON journal append + download and Clear journal unchanged; paths use lib/quiz-export/journal and clipboard per current tree"
  artifacts:
    - path: "frontend/src/components/ExportPanel.tsx"
      provides: "View preview region + in-panel Copy; notes + JSON actions preserved"
    - path: "frontend/src/lib/quiz-export/clipboard.ts"
      provides: "buildQuizClipboardText (reuse; no API change required unless preview needs memo)"
  key_links:
    - from: "frontend/src/components/ExportPanel.tsx"
      to: "frontend/src/lib/quiz-export/clipboard.ts"
      via: "buildQuizClipboardText"
      pattern: "named import"
---

<objective>
Refine **EXP-01** UX: users **view** formatted plain text first, then **copy** from the same preview panel. Aligns with current frontend layout (`quiz-export/journal.ts`, `quiz-export/clipboard.ts`, `types/quiz.ts`, `types/export-journal.ts`, `reviewGeneration` key on `ExportPanel`).

Purpose: Reduce blind clipboard copies; match mental model “preview → copy”.
Output: `npm run build` passes; manual smoke on review screen.
</objective>

<context>
@frontend/src/components/ExportPanel.tsx
@frontend/src/lib/quiz-export/clipboard.ts
@frontend/src/lib/quiz-export/journal.ts
@.cursor/rules/frontend/guidelines.mdc
@.cursor/rules/clean-code/comments.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: View preview + in-panel Copy</name>
  <files>frontend/src/components/ExportPanel.tsx</files>
  <action>
    - Add local boolean state e.g. `isPlainTextPreviewOpen` (or `showPlainTextView`), default **false**.
    - Replace always-visible primary **Copy plain text** with a **View plain text** (or **Preview**) control that toggles the preview **open**. When closed, optionally show a short hint that preview includes the exact string that will be copied.
    - When **open**, render a **view panel** (same `card` / bordered region as rest of app): scrollable `<pre>` or `div` with `whitespace-pre-wrap`, `text-sm`, readable contrast — content from **`buildQuizClipboardText(quiz, topic, comments)`** (compute with `useMemo` when open to avoid extra work when closed, or on each open only).
    - Place **Copy** button **inside** this panel (secondary or primary per visual hierarchy); reuse existing clipboard success/error handling (`role="alert"`, “Copied!” ~2s). Do **not** use `dangerouslySetInnerHTML`.
    - Include **Hide** / **Close preview** (or toggle View again) to collapse the panel; `aria-expanded` on the View toggle if it is a single button.
    - **GUIDE-01**: handlers in `useCallback`; **GUIDE-02** hook order (`useState` → `useMemo`/`useCallback` → effects if any).
    - Keep per-question notes, **Append &amp; download JSON**, **Clear journal** behavior and copy unchanged.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run build</automated>
    <manual>Generate quiz → add notes → View shows text with notes → Copy from panel → paste verifies; Hide collapses; JSON download still works.</manual>
  </verify>
  <done>Preview + in-panel Copy shipped; journal path untouched except imports if needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clipboard | Same as today — secure context; user gesture from in-panel Copy |

## STRIDE (lightweight)

| Threat | Mitigation |
|--------|------------|
| XSS from quiz text | React text nodes only in preview |
</threat_model>

<verification>
- `cd frontend && npm run build`
- Manual: View → preview matches clipboard payload; Copy from panel; JSON append unchanged
</verification>

<success_criteria>
**FE-08** satisfied: preview-before-copy for plain text export; **EXP-01** still satisfied with clearer UX.
</success_criteria>

<output>
After completion, create `.planning/phases/05-export/05-03-SUMMARY.md` and mark **FE-08** complete in `REQUIREMENTS.md` / `ROADMAP.md` progress.
</output>
