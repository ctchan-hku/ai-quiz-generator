---
phase: 05-export
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/QuizDisplay.tsx
  - frontend/src/components/JournalSidebar.tsx
  - frontend/src/components/ExportPanel.tsx
autonomous: true
requirements: [FE-08, EXP-01, EXP-02, EXP-03]
must_haves:
  truths:
    - "User can enter comments directly under each question in the review view"
    - "User can view the export journal and export actions in a side panel"
    - "User can preview and copy the quiz with comments to the clipboard"
  artifacts:
    - path: "frontend/src/components/JournalSidebar.tsx"
      provides: "Export and journal actions"
      exports: ["JournalSidebar"]
    - path: "frontend/src/App.tsx"
      provides: "Two-column layout and shared comment state"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/QuizDisplay.tsx"
      via: "passes comments and onCommentChange props"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/JournalSidebar.tsx"
      via: "passes quiz, topic, and comments props"
---

<objective>
Redesign the UI layout to move comments directly to each question in the review view, and extract the export/journal actions into an always-visible sidebar.

Purpose: Improves UX by attaching comments directly to the context (the questions) and making the export journal always visible on desktop without scrolling to the bottom.
Output: Two-column layout in `App.tsx`, updated `QuizDisplay.tsx` with textareas, and a new `JournalSidebar.tsx` replacing `ExportPanel.tsx`.
</objective>

<execution_context>
@C:/Users/noahc/Documents/projects/talic/mastery-exec/.cursor/rules/frontend/guidelines.mdc
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ExportPanel to JournalSidebar</name>
  <files>frontend/src/components/JournalSidebar.tsx, frontend/src/components/ExportPanel.tsx</files>
  <read_first>
    - frontend/src/components/ExportPanel.tsx
  </read_first>
  <action>
    Create `frontend/src/components/JournalSidebar.tsx` by migrating the logic from `ExportPanel.tsx`.
    1. Define `JournalSidebarProps` with `quiz: QuizResponse`, `topic: string`, and `comments: string[]`.
    2. Copy all imports, state (except `comments` and `handleCommentChange`), and functions (`handleRecordToJournal`, `handleExportJournal`, `handleClearJournal`, `handleCopyFromPreview`, `handleRemoveFromJournal`, `handleToggleJournalItem`, `handleToggleQuizSummaryPreview`, `refreshJournal`) from `ExportPanel.tsx`.
    3. The root element must be `<aside className="w-full md:w-80 lg:w-96 shrink-0 flex flex-col gap-6">`.
    4. Inside the `<aside>`, render the `section.card` containing the export actions, preview toggle, preview panel, and journal list. 
    5. Remove the `ExportCommentRow` mapping and `getPreviewLabel` function since comments are now handled in `QuizDisplay`.
    6. Delete `frontend/src/components/ExportPanel.tsx`.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/JournalSidebar.tsx` exists and exports `JournalSidebar`.
    - `frontend/src/components/ExportPanel.tsx` is deleted.
    - `JournalSidebar` does not contain local `comments` state or `ExportCommentRow`.
  </acceptance_criteria>
  <verify>
    <automated>ls frontend/src/components/JournalSidebar.tsx &amp;&amp; ! ls frontend/src/components/ExportPanel.tsx</automated>
  </verify>
  <done>JournalSidebar created and ExportPanel deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Add comment textareas to QuizDisplay</name>
  <files>frontend/src/components/QuizDisplay.tsx</files>
  <read_first>
    - frontend/src/components/QuizDisplay.tsx
  </read_first>
  <action>
    Update `frontend/src/components/QuizDisplay.tsx`:
    1. Add `comments: string[]` and `onCommentChange: (index: number, value: string) => void` to `QuizDisplayProps`.
    2. Inside the `q.options.map` loop's parent `<article>`, add a comment section at the bottom (after the explanation block).
    3. The comment section should be:
    ```tsx
    <div className="mt-4 border-t border-[rgb(30_41_59/0.1)] pt-4">
      <label className="mb-1 block text-sm font-bold text-[var(--color-text)]" htmlFor={`comment-${qIdx}`}>
        Comment
      </label>
      <textarea
        id={`comment-${qIdx}`}
        className="input min-h-[4.5rem] resize-y w-full"
        value={comments[qIdx] ?? ""}
        onChange={(e) => onCommentChange(qIdx, e.target.value)}
        placeholder="Optional comment…"
        rows={2}
      />
    </div>
    ```
  </action>
  <acceptance_criteria>
    - `QuizDisplayProps` includes `comments` and `onCommentChange`.
    - A `<textarea>` is rendered for each question with `value={comments[qIdx] ?? ""}`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "onCommentChange" frontend/src/components/QuizDisplay.tsx</automated>
  </verify>
  <done>QuizDisplay renders comment textareas.</done>
</task>

<task type="auto">
  <name>Task 3: Lift state and update layout in App.tsx</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/components/JournalSidebar.tsx
  </read_first>
  <action>
    Update `frontend/src/App.tsx`:
    1. Import `useEffect` and `useCallback` from 'react'.
    2. Replace `ExportPanel` import with `JournalSidebar`.
    3. Add state: `const [comments, setComments] = useState<string[]>([])`.
    4. Add effect to reset comments on new quiz:
    ```tsx
    useEffect(() => {
      if (state.quiz) {
        setComments(state.quiz.questions.map(() => ""))
      }
    }, [state.quiz])
    ```
    5. Add handler:
    ```tsx
    const handleCommentChange = useCallback((index: number, value: string) => {
      setComments((prev) => {
        const next = [...prev]
        next[index] = value
        return next
      })
    }, [])
    ```
    6. Change the root `<main>` to a `<div>`: `<div className="mx-auto flex min-h-svh max-w-7xl flex-col md:flex-row gap-6 px-4 py-8 md:px-6 lg:px-8 lg:py-10">`.
    7. Wrap the main content (header, QuizForm, EmptyState, LoadingState, QuizDisplay, ErrorState) in `<main className="flex-1 flex flex-col gap-6 min-w-0">`.
    8. Update the `QuizDisplay` usage: `<QuizDisplay quiz={state.quiz} comments={comments} onCommentChange={handleCommentChange} />`.
    9. Remove the `<div className="flex flex-col gap-6">` wrapper around `QuizDisplay` and `ExportPanel`.
    10. Render `<JournalSidebar key={state.reviewGeneration} quiz={state.quiz} topic={topic} comments={comments} />` outside the `<main>` but inside the root `<div>` when `state.status === 'reviewing' && state.quiz`.
  </action>
  <acceptance_criteria>
    - `App.tsx` root element is a `div` with `max-w-7xl flex-col md:flex-row`.
    - `App.tsx` contains `const [comments, setComments] = useState<string[]>([])`.
    - `JournalSidebar` is rendered as a sibling to `<main>`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "JournalSidebar" frontend/src/App.tsx</automated>
  </verify>
  <done>App layout updated and state lifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> LocalStorage | Export journal is stored in local storage, which is accessible to any script on the same origin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | LocalStorage | accept | Journal data is stored locally on the user's device. No sensitive PII is expected in quiz content. |
| T-05-02 | Tampering | LocalStorage | mitigate | `loadJournal` handles parsing errors gracefully and returns an empty journal if corrupted. |
</threat_model>

<verification>
- `npm run build` succeeds without TypeScript errors.
- The UI layout displays a sidebar on desktop and stacks on mobile.
- Comments entered in `QuizDisplay` are correctly passed to `JournalSidebar` and included in the export.
</verification>

<success_criteria>
- The `ExportPanel` component is successfully replaced by `JournalSidebar`.
- Comments are attached directly to questions in the `QuizDisplay`.
- The app uses a responsive two-column layout.
</success_criteria>

<output>
After completion, create `.planning/phases/05-export/05-05-SUMMARY.md`
</output>
