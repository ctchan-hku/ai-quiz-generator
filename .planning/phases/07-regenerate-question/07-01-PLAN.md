---
phase: 07-regenerate-question
plan: 01
title: "Backend — POST /api/generate/question + single-MCQ parser + shared rate limit"
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/generate.py
  - backend/app/services/llm.py
  - backend/app/services/parser.py
  - README.md
  - backend/tests/ (new or extended)
autonomous: true
requirements:
  - AI-RG-01
  - AI-RG-02
  - AI-RG-03
  - AI-RG-04
  - AI-RG-05
---

<objective>
Add **`POST /api/generate/question`** (same path pattern as **`/api/generate/text`**) with body **`model`**, **`topic`**, **`question`** (full `MultipleChoiceQuestion`), optional **`comment`** — no quiz list, no index, no siblings. Return `{ "question": <MultipleChoiceQuestion> }`. Build user/system messages per **07-CONTEXT** (D-7-04, D-7-06, D-7-07), run LLM with `json_object` + `parse_single_mcq_with_retry`, and share the **3/hour** IP limit with `generate_text` via `shared_limit`.
</objective>

<tasks>

<task id="1">
<type>execute</type>
<title>Pydantic request/response + validation</title>
<read_first>
- backend/app/routers/generate.py
- backend/app/models/schemas.py — `MultipleChoiceQuestion`
- .planning/phases/07-regenerate-question/07-CONTEXT.md (D-7-01 … D-7-03, D-7-02 response)
</read_first>
<action>
Add `GenerateQuestionRequest` (or `RefineQuestionRequest`) in **`generate.py`** next to `GenerateTextRequest`:

- `model: str`, `topic: str` (1…2000), `question: MultipleChoiceQuestion`, `comment: str | None = None` — **max 2_000** for `comment` after trim; empty/missing `comment` is valid (server uses **D-7-04** neutral phrasing in the LLM user message)
- **No** `questions`, `question_index`, or `instruction`

Add `GenerateQuestionResponse` with `question: MultipleChoiceQuestion` (**D-7-02** / AI-RG-02).

Route-level **422** if `model` not in `settings.available_model_ids` (same message style as `generate_text`).
</action>
<acceptance_criteria>
- Invalid model or oversize/invalid `question` or `comment` → **422** with actionable detail
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>build_generate_question_messages + completion kwargs</title>
<read_first>
- backend/app/services/llm.py
- 07-CONTEXT.md (D-7-04, D-7-06, D-7-07, D-7-08)
</read_first>
<action>
Add a dedicated **system prompt** for **one** `MultipleChoiceQuestion` JSON object (not a `questions` array), aligned with the MCQ schema used elsewhere.

Add `build_generate_question_messages(topic, question, comment: str | None) -> list[dict]` (signature adjustable):

- **System:** single-MCQ output rules
- **User message (D-7-06):** **topic**; full **target** `question` (structure clearly — stem, options, correct_indices, explanation); then **D-7-04** text — either neutral rewrite hint or **“Editor comment:”** + trimmed comment. **No** sibling lines, **no** other slots.

**D-7-07:** do not inject few-shot examples.

`complete_single_mcq` helper: `chat.completions.create` with `model` and kwargs from **`CHAT_COMPLETION_KWARGS`** with a **reduced** `max_tokens` constant suitable for one MCQ (e.g. 1_200–1_500), documented in code.
</action>
<acceptance_criteria>
- Unit test (recommended): message builder output includes topic, serialized target question, and neutral vs comment blocks for `comment` `None` vs non-empty; **assert no** “sibling” or multi-question context strings
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>parse_single_mcq_with_retry</title>
<read_first>
- backend/app/services/parser.py — `parse_with_retry`
- 07-CONTEXT.md (D-7-05)
</read_first>
<action>
Implement `parse_single_mcq_with_retry(raw, client, messages, *, chat_completion_kwargs) -> MultipleChoiceQuestion` as in **07-CONTEXT** **D-7-05**; second failure **502**. Reuse `log_full_chat_messages` on the retry path.
</action>
<acceptance_criteria>
- `pytest` covers valid parse and retry path (mock client acceptable)
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>
<title>Route handler + shared limiter</title>
<read_first>
- backend/app/routers/generate.py
- 07-RESEARCH.md (shared_limit)
- 07-CONTEXT.md (D-7-09)
</read_first>
<action>
- Replace `generate_text`’s `@limiter.limit("3/hour")` with `@limiter.shared_limit("3/hour", scope="openai_generate_quota")`.
- New handler: **`@router.post("/generate/question", response_model=GenerateQuestionResponse)`** — full path **`/api/generate/question`**, same `shared_limit` and scope, `Request` first, `Depends(get_llm_client)`.
- Wire: validate body → `build_generate_question_messages` → complete → `parse_single_mcq_with_retry` → return `GenerateQuestionResponse`.

Document the shared **3/hour** bucket across **`/api/generate/text`** and **`/api/generate/question`** in **`README.md`** if rate limits are already documented.
</action>
<acceptance_criteria>
- OpenAPI lists **`POST /api/generate/question`**; `pytest` passes
</acceptance_criteria>
</task>

</tasks>

<verification>
**Maps to 07-CONTEXT:** D-7-01…D-7-09 (API + LLM + limiter; no UI decisions).

**Maps to REQUIREMENTS:** AI-RG-01…05
</verification>
