---
phase: 07-regenerate-question
plan: 02
title: "Frontend — version stacks, refine UI, API, export from selected versions"
wave: 2
depends_on: [07-01-PLAN.md]
files_modified:
  - frontend/src/types/quiz-machine.ts
  - frontend/src/types/api.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useQuizMachine.ts
  - frontend/src/App.tsx
  - frontend/src/components/QuizDisplay.tsx
  - frontend/src/components/CurrentQuizActions.tsx
autonomous: true
requirements:
  - FE-RG-01
  - FE-RG-02
  - FE-RG-03
  - FE-RG-04
  - FE-RG-05
  - FE-RG-06
  - FE-RG-07
---

<objective>
Add client-side **version stacks** per question, **version `<select>`**, and a **refine** flow that calls **`POST /api/generate/question`** with **`model`**, **`topic`**, the **resolved** **`question`**, and optional **`comment`** (review textarea) — no full-quiz array in the request. Derive **display + export** from **selected** versions. Depends on backend **07-01-PLAN**.
</objective>

<tasks>

<task id="1">
<type>execute</type>
<title>Types + `generateQuestion` (or `refineQuestion`) API client</title>
<read_first>
- frontend/src/types/api.ts
- frontend/src/lib/api.ts
- frontend/src/types/quiz.ts
- 07-CONTEXT.md (D-7-02, D-7-10, D-7-11, D-7-12)
</read_first>
<action>
Add request/response types:

- `GenerateQuestionRequest`: `model`, `topic`, `question: MultipleChoiceQuestion`, `comment?: string`
- `GenerateQuestionResponse`: `{ question: MultipleChoiceQuestion }`

Add `generateQuestion` (or `refineQuestion`) in `api.ts`: **`POST /api/generate/question`**, return `data.question`.

**Payload:** For index `i`, `question` = **resolved** MCQ for that slot (`versions[i][selectedVersionIndex[i]]`). `comment` = `comments[i]` (trim or raw per UX). `model` = `resolvedModel`, `topic` = App `topic` (**D-7-10**, **D-7-11**).
</action>
<acceptance_criteria>
- `npx tsc --noEmit` in `frontend/` passes
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>useQuizMachine — version state + reducers</title>
<read_first>
- frontend/src/hooks/useQuizMachine.ts
- frontend/src/types/quiz-machine.ts
- 07-CONTEXT.md (D-7-16…D-7-20)
</read_first>
<action>
Same as prior plan: `questionVersions`, `selectedVersionIndex`, `GENERATE_SUCCESS` init, `APPEND_QUESTION_VERSION`, `SET_QUESTION_VERSION`, `buildResolvedQuizResponse` helper; single source of truth for resolved quiz (see **07-01**-era **07-02** text if needed — logic unchanged; only the **refine** API call shape changes).
</action>
<acceptance_criteria>
- Reducer and `tsc` as before
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>App + QuizDisplay — resolved quiz, refine + version UI</title>
<read_first>
- frontend/src/App.tsx
- frontend/src/components/QuizDisplay.tsx
- 07-CONTEXT.md (D-7-12, D-7-13, D-7-18, D-7-19)
</read_first>
<action>
- Pass **resolved** `quiz` from `buildResolvedQuizResponse` to `QuizDisplay` and **CurrentQuizActions**.
- **Refine (D-7-12):** **Confirm** calls **`POST /api/generate/question`** with `question` = that card’s resolved MCQ, `comment` = textarea, **no** second instruction field.
- **D-7-13:** per-question loading; **D-7-18** / **D-7-19** as in **07-CONTEXT** (version select, do not clear comment by default on success).
</action>
<acceptance_criteria>
- Version switch and refine work; no request sends a full `questions[]` list
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>
<title>CurrentQuizActions — resolved quiz</title>
<read_first>
- frontend/src/components/CurrentQuizActions.tsx
- frontend/src/lib/quiz-export/*
- 07-CONTEXT.md (D-7-15, D-7-20)
</read_first>
<action>
Ensure clipboard/journal use the same **resolved** `QuizResponse` as the cards (**D-7-20**). Session-only version history UAT note unchanged.
</action>
<acceptance_criteria>
- Export matches on-screen **selected** versions
</acceptance_criteria>
</task>

</tasks>

<verification>
**07-CONTEXT map:** D-7-10…D-7-20 (UI + client state, no sibling API fields).

**REQUIREMENTS map:** FE-RG-01…FE-RG-07
</verification>
