# Environment config for this repo:
#
# Local Docker dev  → cp .env.dev.example .env.dev  (secrets only; gitignored)
# Dev overrides     → docker-compose-dev.yml
# Defaults          → app/config.py  (single source of truth)
# HKU testing/prod  → GEAR Infisical via gi20secrets sidecar (see .env.prod)
