# Secrets for local Docker dev. Copy to .env.dev (gitignored).
# Non-secret config lives in docker-compose-dev.yml.
# Production secrets live in Infisical (gi20secrets sidecar).
#
# Start: docker compose -f docker-compose-dev.yml up --build
# Requires GEAR dev stack on gi20-shared-network (gi20db-dev).
# ACCESS_TOKEN_SECRET must match GEAR dev backend.

OPENAI_API_KEY=
ACCESS_TOKEN_SECRET="d6#.4w5^:Y£(£Pxt%]JI1)+'n`z'Zi69DKc&mH74.@c{lB;C:T"
MONGODB_URI=mongodb://root:donutCHURROSwaffle@gi20db-dev:27017/?replicaSet=rs0&authSource=admin
HF_TOKEN=
