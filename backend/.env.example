# Environment config for this repo:
#
# Local Docker dev  → cp .env.dev.example .env.dev  (fill in values; gitignored)
# Non-secret dev    → docker-compose-dev.yml
# HKU production    → GEAR Infisical via gi20secrets sidecar (not .env files)
