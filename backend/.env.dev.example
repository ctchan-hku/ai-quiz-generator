# Copy to .env.dev (gitignored). Used by docker compose -f docker-compose-dev.yml
#
# Requires GEAR dev stack on gi20-shared-network (gi20db-dev).
# Get ACCESS_TOKEN_SECRET and MONGODB_URI from the GEAR dev team / stack.

OPENAI_API_KEY=
ACCESS_TOKEN_SECRET=
MONGODB_URI=
HF_TOKEN=
