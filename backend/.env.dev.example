# Secrets only — copy to .env.dev and fill in values (gitignored).
# Non-secret dev config: docker-compose-dev.yml
# Defaults: app/config.py

OPENAI_API_KEY=
ACCESS_TOKEN_SECRET=
MONGODB_URI=
# HF_TOKEN=
