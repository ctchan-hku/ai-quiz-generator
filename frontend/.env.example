# Absolute API origin (e.g. https://your-app.up.railway.app). Leave unset in local dev to use Vite proxy → http://127.0.0.1:8000.
# VITE_API_BASE_URL=

# Must match an id from the backend AVAILABLE_MODELS list.
VITE_DEFAULT_MODEL=gpt-4o-mini
