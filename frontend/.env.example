# Absolute API origin (e.g. https://your-app.up.railway.app — no :port; Railway serves HTTPS on 443). Leave unset in local dev to use Vite proxy → http://127.0.0.1:8080.
# VITE_API_BASE_URL=

# Models are loaded from GET /api/models (see AVAILABLE_MODELS on the backend).
