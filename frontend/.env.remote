# Loaded when you run `npm run dev:remote` (vite --mode remote).
# Public Railway development host — override without editing this file by creating `.env.remote.local` (gitignored).
# If your Railway service still uses the old slug, point this at that hostname until you rename the service.
VITE_API_BASE_URL=https://ai-test-generator-development.up.railway.app
